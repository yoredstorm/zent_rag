[project]
name = "rag-as-a-service"
version = "0.1.0"
description = "RAG-as-a-Service Platform — AI Agent Orchestration with Full Observability"
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [
    { name = "RAG Platform Team" }
]

[project.optional-dependencies]
prod = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.0.0",
    "pydantic-settings>=2.0.0",
    "sqlalchemy[asyncio]>=2.0.30",
    "asyncpg>=0.29.0",
    "psycopg2-binary>=2.9.0",
    "redis[hiredis]>=5.0.0",
    "litellm>=1.40.0",
    "qdrant-client>=1.9.0",
    "structlog>=24.0.0",
    "prometheus-fastapi-instrumentator>=7.0.0",
    "prometheus-client>=0.20.0",
    "httpx>=0.27.0",
    "alembic>=1.14.0",
    "sqlglot>=26.0.0",
    "hvac>=2.0.0",
    "opentelemetry-api>=1.30.0",
    "opentelemetry-sdk>=1.30.0",
    "opentelemetry-exporter-otlp>=1.30.0",
    "opentelemetry-instrumentation-fastapi>=0.51b0",
]

dev = [
    "ruff>=0.5.0",
    "mypy>=1.10.0",
    "bandit>=1.7.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-timeout>=2.0.0",
    "httpx>=0.27.0",
    "pyright>=1.1.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = [
    "E", "F", "W",       # pyflakes/pep8
    "B",                  # flake8-bugbear
    "S",                  # bandit (security)
    "I",                  # isort
    "T20",                # no prints
]
ignore = [
    "B008",   # Depends in defaults is FastAPI convention
    "B904",   # raise ... from err not always needed
    "B905",   # zip() strict not needed in controlled contexts
    "S101",   # assert is fine in tests
    "S104",   # 0.0.0.0 is explicit bind in docker
    "S106",   # "token_type" false positive
    "S110",   # try/except/pass is intentional in some cases
    "F841",   # unreferenced variable false positives
]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101", "E402", "E501"]
"src/api/main.py" = ["E402"]  # sys.path.insert before imports
"src/api/routes/prompt.py" = ["E501"]  # long description strings
"src/application/orchestrator.py" = ["E501"]  # prompt string constants
"src/api/metrics.py" = ["F401"]  # Info imported for registry side-effect
"src/infrastructure/evaluation.py" = ["S608"]
"src/infrastructure/sql_expert.py" = ["E501", "S608", "B007"]  # prompt strings, validated SQL, FK chains loop
"src/api/routes/admin.py" = ["S608", "F841"]  # validated identifiers, dev-only
"src/infrastructure/relational_db.py" = ["E402", "S608"]
"src/infrastructure/data_ingestion.py" = ["S608"]

[tool.ruff.lint.isort]
known-first-party = ["src"]

[tool.mypy]
python_version = "3.11"
strict = false
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
ignore_missing_imports = true

[tool.bandit]
exclude_dirs = ["tests", ".venv"]
skips = ["B101", "B603"]  # assert (tests) y subprocess sin shell=True

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
