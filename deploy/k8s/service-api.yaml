apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app.kubernetes.io/name: zent
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
