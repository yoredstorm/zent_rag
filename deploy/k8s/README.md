# Kustomize overlay

Render (no cluster):

```bash
kustomize build .
```

Cuándo aplicar: [docs/platform/KUBERNETES.md](../../docs/platform/KUBERNETES.md). Postgres/Qdrant/Redis no van en este overlay.
