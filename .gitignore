# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Environment
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# Docker / local overrides
docker-compose.override.yml

# Runtime tenant data (exports, reports, backups, DSR, agent migrations)
# Root only — portal/src/pages/onboarding/data is source code and must be tracked
/data/
uploads/
# Dumps de Postgres para verificación/rollback local (pg_dump)
/backups/

# Construction / internal docs (producto: solo README.md raíz)
docs/*
# Architecture ADRs are product artifacts (Enterprise Knowledge Engine Phase A+)
!docs/architecture/
!docs/architecture/**

# Portal
portal/node_modules/
portal/dist/
portal/shots/

# SDKs
sdk/node/node_modules/
sdk/node/dist/
sdk/python/.pytest_cache/

# Caches / coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/

# tgrep local index (microsoft/tgrep, installed under .agents/tools/tgrep)
.tgrep/
htmlcov/
.coverage
coverage.xml
coverage.json

# Agent skills (local tooling)
.agents/
skills-lock.json

# Local scratch requests (contain webhook secrets)
bruno/RAG/prueba.bru

# Isolated git worktrees + SDD scratch
.worktrees/
.superpowers/

# Logs
*.log
AGENTS.md